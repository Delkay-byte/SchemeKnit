# TeachFlow Dependencies

# Core
# fastapi + starlette must be pinned together (see desktop/build-requirements.txt).
fastapi==0.141.1
starlette==1.6.0
uvicorn[standard]==0.30.6
python-multipart==0.0.9
pydantic==2.9.2
pydantic-settings==2.5.2
python-dotenv==1.0.1

# Database
sqlalchemy==2.0.35
aiosqlite==0.20.0
# PostgreSQL production driver (psycopg v3). Required when DATABASE_URL uses postgresql://.
psycopg[binary]==3.3.5

# Authentication
passlib[bcrypt]==1.7.4
python-jose[cryptography]==3.3.0
bcrypt==4.2.0

# Document processing
python-docx==1.1.2
docxtpl==0.16.0
pymupdf==1.28.2
openpyxl==3.1.5
pandas==2.2.3

# PDF conversion (optional — requires MS Word or LibreOffice at runtime)
docx2pdf==0.1.8

# File handling
aiofiles==24.1.0
aiohttp==3.10.5

# Object storage (S3-compatible) — only needed when STORAGE_BACKEND=s3
boto3==1.43.97

# HTTP client (for AI providers)
httpx==0.27.2
requests==2.32.3

# Logging
structlog==24.4.0

# Testing
pytest==8.3.3
pytest-asyncio==0.24.0

# Development
black==24.8.0
ruff==0.6.9
