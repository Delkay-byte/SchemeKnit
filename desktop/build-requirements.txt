# TeachFlow Backend - Minimal Dependencies for Desktop Packaging
# No torch, no sklearn, no transformers, no scipy

# Core
# NOTE: keep fastapi + starlette pinned together. fastapi 0.115.0 passes the
# removed `on_startup` kwarg into starlette's Router, which crashes the frozen
# desktop backend on startup (TypeError: Router.__init__() got an unexpected
# keyword argument 'on_startup'). These versions match the working dev venv.
fastapi==0.141.1
starlette==1.6.0
uvicorn[standard]==0.30.6
python-multipart==0.0.9
pydantic==2.9.2
pydantic-settings==2.5.2
python-dotenv==1.0.1

# Database
sqlalchemy==2.0.35
aiosqlite==0.20.0

# Authentication
passlib[bcrypt]==1.7.4
python-jose[cryptography]==3.3.0
bcrypt==4.2.0

# Document processing
python-docx==1.1.2
docxtpl==0.16.0
pymupdf==1.28.2
openpyxl==3.1.5
pandas==2.2.3

# File handling
aiofiles==24.1.0
aiohttp==3.10.5

# HTTP client (for AI providers)
httpx==0.27.2
requests==2.32.3

# Logging
structlog==24.4.0
